[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
# Optional: if you need a specific NODE_VERSION you can set it here
# NODE_VERSION = "18"

[dev]
  command = "npm run dev"
  port = 5173
